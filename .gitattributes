# 統一換行為 LF，避免 Windows 取檔變成 CRLF 導致腳本解析異常
* text=auto eol=lf
*.png binary
*.jpg binary
*.pdf binary
*.xlsx binary
*.docx binary
*.pptx binary

# 統計後端的原始碼：維護者才需要，不放進同事下載的 zip
# （docs/ 不排除——README 有引用 docs/統計設定-給維護者.md，排掉會變死連結）
backend/ export-ignore
