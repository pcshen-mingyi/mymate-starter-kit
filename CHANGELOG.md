# 版本紀錄

## v2.1（2026-08-18）修掉會製造假警報的護欄誤判

**問題**：`find . -name ".git" 2>/dev/null` 這種**純讀取**的指令，會被
`block-delete` 判定為「⚠️ 這個動作可能無法復原：覆寫檔案內容，約 10 個目標」。

兩個獨立的錯誤：

1. `2>`（把錯誤訊息丟掉）跟 `>`（覆寫檔案）被當成同一件事，
   而 `2>/dev/null` 是丟進系統黑洞，**沒有任何檔案被碰到**。
2. 「約 N 個目標」是把整行指令的每個詞都算進去——那十個「目標」其實是
   路徑被空白切碎的片段、`ls`、`echo`、`find`、`3` 之類的東西。

**為什麼要修**：假警報的長期傷害比漏報更大。使用者看過幾次「可能無法復原」
而實際什麼都沒發生之後就不會再讀，然後真正該擋的那一次也會被按過去。
警示的價值來自它很少響。

**修了什麼**

- 判斷邏輯移到 `.claude/hooks/_destructive.mjs`（純函式，可單獨測試）
- 重導向判斷加上三個守衛：前面不是 `>`（排除 `>>` 附加）、後面不是 `>` 或 `&`
  （排除 `2>&1`）、目標不是 `/dev/null` 這類黑洞
- 「會影響什麼」改成列出**真正的目標**；列不出來就明講「無法自動列出」，
  不再生成假數字——具體但錯誤的數字比「無法判斷」更糟，因為它看起來很可信
- 說明文字改寫：講清楚兩個按鈕各自的後果、拒絕不會有損失，
  不再把判斷責任丟回給 AI（原本寫「先拒絕，然後問 Claude 這個指令會刪掉什麼」）
- 新增 `tests/危險動作判斷.test.mjs`：39 項回歸測試，兩個方向都守——
  純讀取指令不得被攔、真正會毀資料的動作必須被攔

**第二個誤判：heredoc 內容被當成指令**

`cat > tests/x.test.mjs <<'EOF' … EOF` 這種建立檔案的指令，會因為**內容裡**
出現 `rm -rf`（測試案例、說明文件都很常寫到）而被判成「強制遞迴刪除，約 235 個目標」。
heredoc 中間那一段是**要寫進檔案的內容**，不是要執行的指令。

- 判斷前先剝掉 heredoc 內容；但接給 shell 執行時（`| bash`）不剝，那時內容真的會跑
- 修正後同一個指令正確判定為「覆蓋檔案內容」，目標是被寫入的那一個檔案

**訊息重排：重點前置、條列為主**

舊版把「完整指令」與四行建議放在中間偏下，使用者得捲到最下面才看到該怎麼做——
而確認視窗本來就會在下半部顯示完整指令，重複一次只是把重點推遠。
對沒有技術背景的人，訊息越長越不會讀，讀不到重點跟沒有警示一樣。

- 第一行就是白話結論（例如「刪掉整個資料夾，連裡面所有東西一起」），移除指令術語
- 其餘壓成兩三個條列：影響範圍、是否可復原、不確定時怎麼辦
- 移除重複的「完整指令」與 MCP 工具的 JSON 內容預覽（小白看不懂，且視窗已有）
- 影響範圍壓成一行，最多列三個目標再說「還有 N 個」
- 三個護欄（`block-delete`／`block-sensitive`／`confirm-send`）統一成同一種版型

## v2.0（2026-08-11）統計後端上線

v1.9 把計數邏輯寫好了但端點留空（＝一筆都不送）。這版把它接成一條會動的線。

**後端**：Apps Script 接收端，綁在統計試算表上，授權範圍只有 `spreadsheets.currentonly`
（只能寫那一份試算表）。只接受 `install`／`uninstall`，其餘一律丟棄；`version`／`platform`
各截斷 20 字元；不寫入 IP、User-Agent 或請求裡的任何其他欄位；任何異常都回 200 不洩漏內部狀態。

**`_stats.mjs`**：填入 `/exec` 端點，`BUNDLE_VERSION` → `2.0`。公開介面未變。

**「統計」分頁**：即時快照、累計折線圖、每日趨勢（連續 60 天、空白日補 0）、
每週彙總（12 週）、版本與平台分布。由 `setup()` 產生，可重複執行。

**修掉的兩個 bug**
- `appendRow` 會讓試算表把 `"2.0"` 判成數字 `2`（`2.10` 與 `2.1` 也會撞值）。
  改用 `setNumberFormats("@")` + `setValues`，版本與平台一律當文字存。
- 趨勢原本用 `QUERY group by toDate()`，只列有資料的日子、跳過空白日，
  曲線會誤導判讀。改成自己產生連續日期軸再逐日 `COUNTIFS`。

**驗證**（沙箱 HOME，未動到開發機的真實設定）
安裝／還原各寫入一列；`--dry-run` 與 `MYMATE_NO_STATS=1` 零送出；
非法 `event`、畸形 JSON、空 body 一律回 200 且不寫入；100 字元欄位截斷成 20。
**端點指向 `example.invalid` 時：安裝仍成功、耗時 2.08s（正常端點 2.12s），
延遲增加 0 秒，錯誤完全靜默。**

## v1.9（2026-08-05）匿名安裝計數

**目的**：`install` +1、`uninstall` +1，作為「要不要進一步訪談或發問卷」的判斷訊號。
**不做**使用者識別、不做行為分析。使用者回饋另外詢問，願意才給。

**只送三個欄位**
`event`（install／uninstall）、`version`、`platform`（darwin／win32）。

**不送**：使用者名稱、機器 ID、檔名、檔案內容、資料夾路徑、對話內容。
**沒有任何識別碼**——因此無法追蹤個人，也無法把安裝與卸載配對。
得到的是「**次數**」不是「人數」，這是刻意的取捨。

**安全與可控**
- 預設**完全關閉**（`STATS_ENDPOINT` 留空 → 不送出任何東西），維護者部署後才啟用。
- `MYMATE_NO_STATS=1` 可關閉，並在訊息中明白告知（沿用 Homebrew／Next.js 等的慣例）。
- `--dry-run` 不送。
- 逾時 2 秒、失敗一律靜默忽略，**永不影響安裝或還原本身**（已實測端點不通仍安裝成功）。
- 安裝與還原結束時**主動顯示**回報內容與關閉方式，不埋在文件裡。

**為什麼採「明白告知的 opt-out」而非靜默回報**
本包的 `explain-code`／`check-skill` 會把「對外送出資料」標為危險訊號。
若我們自己靜默回報，任何人打開 `_stats.mjs` 就會看到矛盾，信任會一次崩掉。
因此設計成：**送什麼寫在程式碼註解、README、安裝訊息三個地方，一致且可查證**——
即使被自己的 skill 標記，說明也站得住腳。

**新增檔案**
- `_stats.mjs`：計數模組（含完整設計說明註解）。
- `docs/統計設定-給維護者.md`：後端設定指引，列出三種選項
  （Apps Script ＋ 試算表／Cloudflare Worker／既有後端），以 Apps Script 為範例；
  明確要求**另建獨立存放位置，不沿用其他專案的既有設定**；含公開端點可被灌水的提醒。
- `README.md`：新增「匿名計數（透明說明）」章節。

**實測**
dry-run 不送 ✓／實際安裝送出 `{"event":"install","version":"1.9","platform":...}` ✓／
還原送出 uninstall ✓／`MYMATE_NO_STATS=1` 不送 ✓／端點不通仍安裝成功且 skills 正確安裝 ✓

## v1.8（2026-08-05）開場定向 ＋ 引導安裝列為首要任務

**背景**
v1.7 依課程 PM 的意見移除了 `my-first-run`。但隨後確認：**課程只是其中一種散布路徑**——
使用者覺得好用會推薦給別人，那些人自己下載、沒有課、沒有人帶。
若什麼都不說，他打開資料夾只會對著空白對話框發呆。

同時，安裝包的核心價值是「讓保護真正生效」。若把安裝當成被動選項，
多數人永遠不會做——等於帶著「以為有保護」的錯覺在用。

**新增：開場定向（三句話，不是教學）**
觸發於對方像是第一次來（問「怎麼開始」「這是什麼」，或第一句很空泛且無具體任務）：
自我介紹＋這裡是什麼 → 可以叫我做哪類事（**2–3 個具體例子**）→ 保護狀況一句話。
**不展開成教學**；若對方一來就有明確任務，先做他的事。

與已移除的 `my-first-run` 的差別：那是 7 步、5–10 分鐘的走查；
這只是開頭三句話，講完就進正題。課程情境下不會打架，自行下載的人也不會卡住。

**變更：引導安裝改為首要任務**
定向後**立刻**確認安裝狀態（`ls ~/.claude/hooks/block-sensitive.mjs`）：
- 尚未安裝 → 主動邀請完成約一分鐘的設定，並說明「隨時可還原、不動你原有設定」。
- 已安裝 → 一句話帶過，直接問想做什麼。

**但主動引導 ≠ 施壓**（保留先前建立的原則）：
仍要列出會裝什麼、說明全機生效的副作用、取得明確同意才動手。
**對方拒絕就尊重，只回一句「之後想裝再說」，不反覆提醒、不每次對話都問。**

**連帶調整**
- `CLAUDE.md`：新增「開場：定向，然後引導安裝（首要任務）」；
  原「不要主動推銷 my-install」改為「首要任務，但只引導一次」。
- `my-install`：description 加入「開場定向後主動引導」的使用時機；
  內文開頭由「不要主動推銷」改為「這是本包的首要任務」，並保留不施壓的界線。
- `README.md`：使用步驟第 3 步改為「打聲招呼就好」，說明會被帶著做一分鐘設定。

## v1.7（2026-08-05）移除 my-first-run，開場引導交還課程

**決策來源**：與負責課程的 PM 討論後確認——第一次使用的走查由**課程**負責，
安裝包不需要再跑一次教學。安裝包的職責收斂為「安裝／解除安裝 ＋ 安全與陪伴」。

**移除**
- `.claude/skills/my-first-run/`（開場引導、環境確認、示範護欄、陪做第一件事）

**保留**（六個 skill）
`my-install`、`my-uninstall`、`plain-speak`、`safety-check`、`explain-code`、`check-skill`。
安全護欄與白話陪伴仍由安裝包提供，不隨引導一起拿掉。

**保留下來的關鍵內容：誠實告知保護現況**
`my-first-run` 裡有一段不屬於「開場引導」、而是**隨時都該成立的原則**——
判斷自動攔阻是否已啟動，並如實告訴使用者目前有哪些保護。
這段移入 `CLAUDE.md` 成為常駐規則，避免隨 skill 一起消失：

- 判準是「hook 裝在哪一層」（檢查 `~/.claude/hooks/block-sensitive.mjs`），**不是作業系統**。
- 尚未安裝時要說明保護來自「CLAUDE.md 規則 ＋ App 確認視窗」，而非技術強制。
- **不主動推銷 `my-install`，但被問到就要講清楚，不含糊帶過。**
- 原則寫明：最糟的情況不是沒有保護，而是「以為有保護、實際上沒有」。

**連帶調整**
- `CLAUDE.md` skill 對照表移除該列，並註明「開場引導由課程負責，本包不做第一次使用走查」。
- `README.md` 的使用步驟第 3 步改為「直接說你想做什麼」，並註明教學在課程進行。
- `my-install` 移除「建議先跑 my-first-run 用幾天」的前置條件（該門檻已於 v0.10 判定多餘，此處清除殘留引用）。

## v1.6（2026-08-05）檔頭說明校正與空殼清理

### 1. 檔頭說明與實際行為不符
`uninstall.mjs` 開頭第 5–6 行仍寫著「從 `settings.mymate-original.json`（安裝前原貌）還原」，
那是 v1.5 之前的做法。程式早已改為**精準移除**，說明卻沒跟著改，
日後閱讀者會被誤導。

**修正**：檔頭改寫為實際行為，並把核心原則寫在最前面——
「**精準移除，不是回到過去**」，同時說明快照仍保留但**不會自動套用**及其原因。

### 2. 還原後會留下空殼
移除本包的權限規則後，`permissions` 可能只剩三個空清單，或整個變成空物件。
功能上無害，但不夠乾淨。

**修正**：清掉被我們清空的清單與空的 `permissions`／`enabledPlugins`／`hooks`。

**但只清「我們建立的」**——安裝時記錄 `createdKeys`（哪些頂層鍵在安裝前本來不存在），
還原時只清這些。若使用者自己原本就有空的 `permissions: {}`，一律保留。
（重複安裝時 `createdKeys` 沿用首次紀錄，不會誤把既有鍵當成我們建的。）

### 實測（三情境皆與安裝前完全一致）
| 情境 | 結果 |
|---|---|
| 全新環境（殼是我們建的） | ✓ 空殼清掉，回到乾淨 |
| 使用者原本有自己的規則 | ✓ 結構與內容都未被誤清 |
| 使用者原本就有空的 `permissions: {}` | ✓ 保留，不被刪除 |

並回歸驗證 v1.5 的關鍵情境：安裝後自行新增的 plugin／hook／規則／欄位全數保留。

## v1.5（2026-08-05）修正還原會抹掉使用者後續設定的風險

### 問題（嚴重，v0.7 引入）
還原從 `settings.mymate-original.json` 套用，但那是**上一次安裝當下**的狀態。
使用者在安裝之後自己新增的東西——新的 hook（petdex）、新的 plugin（warp）、
新的規則、新的欄位——**全部會被抹掉**。

還原變成了「時光機」，而不是「拆掉我們裝的東西」。這是真正的資料遺失風險。
（v0.7 為了解決「重複安裝後還原失效」而引入快照還原，卻造成了更嚴重的問題。）

### 修正：改為與安裝對稱的「精準移除」
安裝是「只加我們的」，還原就該是「**只移除我們的**」，而不是回到某個時間點。

- `install.mjs` 的 `mergeSettings()` 現在同時回傳 **`added`**——
  精確記錄「這次實際新增了哪些 plugin／規則／是否設了 autoInstallEnabledPlugins」，
  寫入 `.mymate-manifest.json` 的 `settingsAdded`（重複安裝時做聯集）。
- `uninstall.mjs` 改為**從當前的 settings.json 逐項移除紀錄在案的項目**，
  其餘一律不動。hooks 仍以本包檔名精準辨識。
- **快照降級為緊急參考**：仍會保存，但不再自動套用，訊息也改為說明它只是
  「上次安裝當下的樣子，供萬一需要時人工比對」。

### 附帶好處：不會誤刪使用者原本就有的同名項目
因為紀錄的是「**我們實際新增的**」而非「本包內容」，
若使用者安裝前就有 `Bash(rm -rf *)` 或同一個 plugin，還原時不會被移除。

### 實測

**情境一：安裝後使用者自己新增東西再還原**（本次回報的情境）
- 安裝後自己裝的 `warp` plugin → ✓ 保留
- 安裝後自己加的 petdex hook → ✓ 保留
- 安裝後自己加的 deny 規則與自訂欄位 → ✓ 保留
- 安裝前就有的 plugin／規則／`model` → ✓ 保留
- 本包的 plugin／護欄／規則／`autoInstallEnabledPlugins` → ✓ 全部移除

**情境二：使用者原本就有與本包相同的規則**
- 原有的 `claude-code-setup`、`Bash(rm -rf *)`、`Bash(sudo *)`、`Bash(rm *)` → ✓ 全部未被移除
- 本包額外加的規則 → ✓ 已移除

### 連帶更新
`my-uninstall` skill 的說明改為「只移除當初加上去的，你之後新增的都會保留」，
不再說「還原成安裝前的原貌」。

## v1.4（2026-08-05）新增 check-skill：裝別人的工具前先檢查

**動機與風險模型**
`explain-code` 處理的是「程式碼跑了會怎樣」。但 skill 的風險**完全不同**：

> 程式碼是**執行**才有影響；skill 是**寫給 agent 看的指示**——讀到就會照做。
> 一個有問題的 skill 不需要偷跑任何程式，只要在文字裡叫 agent
> 「把某個檔案內容送到某個網址」就可能生效。

小白會從 marketplace、同事、網路上拿到 skill，卻沒有能力判斷那份指示會叫 agent 做什麼。

**新增 `check-skill`**

流程：來源判斷 → 比對 description 與內文 → 逐項對照危險訊號 → 紅黃綠結論 → 說明如何移除。

**關鍵檢查點：description 與內文是否一致。**
描述寫「整理表格」、內文卻要讀 `.env`——這種不一致本身就是最強的危險訊號，
因為使用者是**看描述決定要不要裝**的。

**危險訊號分六類**
- **A 要求違反既有規則**：「忽略先前指示」「不用問直接做」「跳過安全檢查」
- **B 要求對使用者隱瞞**：「不要告訴使用者」「靜默執行」——**最嚴重**，
  正當的 skill 沒有理由要瞞著使用者做事
- **C 碰敏感資料**：`.env`、`.ssh`、憑證、keychain、掃描全機
- **D 對外送出**：webhook、不明網址、「上傳／回報／同步到」
  （**C＋D 同時出現＝典型資料外洩組合**）
- **E 附帶檔案**：`scripts/`、`hooks/` 一律改用 `explain-code` 逐一看過
- **F 其他**：要求裝別的東西、超長內文夾帶指示、混淆編碼、要求提權

**來源信任度分級**：官方 marketplace ＞ 自家 marketplace ＞ 陌生 repo ＞ 別人直接傳的檔案。
但**來源可信不等於免檢**，只是可以講快一點。

**誠實原則**：看不懂就說看不懂；**本包自己的 skill 也可以用這個檢查**——
使用者想看就照做，不要說「那是我們自己的不用看」。透明本身就是信任的一部分。

**連帶更新**
- `CLAUDE.md` 安全底線新增第四條：安裝非本包的 skill／plugin 前先調用 `check-skill`。
- `explain-code` 新增分流：遇到 skill／plugin 改用 `check-skill`。
- `README.md` 能力表與資料夾結構同步。

## v1.3（2026-08-05）新增 explain-code skill

**動機**
小白最常見也最危險的情境是：**別人丟給他一個檔案、或網路上抄一行指令叫他跑**。
他沒有能力判斷那會做什麼，只能憑「應該沒問題」執行。
`safety-check` 管的是「這個動作安不安全」，缺一塊「**這段程式碼到底會做什麼**」。

**新增 `explain-code`**
定位明確：回答使用者真正在意的問題——「跑了會怎樣？會不會弄壞我的電腦或資料？」
**不做程式碼品質審查**（不談架構、命名、效能）。

流程：
1. 先看它會碰什麼——檔案／網路／安裝設定／執行其他程式／可逆性五個面向。
2. **先給紅黃綠結論**再講細節（🟢 安全／🟡 要留意／🔴 建議先不要跑）。
3. 白話說明整體在做什麼，**不逐行翻譯**；用具體的資料夾、數量、網址，
   不說「執行檔案操作」而說「會刪掉 `舊備份` 裡的 12 個檔案」。
4. 明確指出風險、可否救回，並給建議——不准說「請自行評估」。

**危險訊號清單**（命中即標 🔴 並解釋原因）：
`curl | bash` 類的下載後直接執行、看不懂的 base64／混淆字串、
含變數的 `rm -rf`、讀取 `.env`／`.ssh`／憑證、來歷不明的網址、
要求 sudo、會排程或開機執行、關閉安全機制（跳過憑證驗證等）。

**誠實原則**：看不懂就說看不懂，不要編一個合理的解釋；
不因為「來源可信」而降低標準。

**三個常見情境的處理**：同事傳的檔案、網路上抄的指令、
以及**本包自己的腳本**（如 `install.mjs` 也要說明，不因為是自己的就跳過）。

**連帶更新**
- `CLAUDE.md` 安全底線新增第三條：執行不熟悉的程式碼前先調用 `explain-code`。
- `safety-check` 的 D 類交叉引用 `explain-code`。
- `README.md` 能力表與資料夾結構同步。

## v1.2（2026-08-05）還原時誠實交代已下載的 plugin

### 1. 沒有處理自動下載的 plugin（誠實性問題）
`install.mjs` 會寫入 `autoInstallEnabledPlugins: true`，App 重啟後 Claude 會把 plugin
**實際下載**到 `~/.claude/plugins/cache/` 並登記進 `installed_plugins.json`。
但還原只清掉 settings 裡的開關，**檔案與登記都留著**，訊息卻只輕描淡寫說
「plugin 需另外自行移除」——沒告訴使用者它其實已經被下載了。

**修正**：新增 `detectDownloadedPlugins()`，還原時主動掃描登記檔與下載快取，
若偵測到本包帶進來的 plugin，明確列出**名稱與實際位置**，並說明：

- 這些是 Claude 自己管理的檔案，本工具**不會代為刪除**（以免弄壞 plugin 狀態）
- 要移除請執行 `/plugin uninstall <名稱>`（附上完整指令）
- settings 的開關已隨還原移除，重開後不會再自動下載

沒偵測到時不再無意義地印那句提示。

### 2. 刪除清單會出現 `.DS_Store`（顯示問題）
`uninstall.mjs` 的 fallback 分支有過濾垃圾檔，但**讀 manifest 的分支沒有**——
舊版安裝（v1.0 之前尚未過濾）寫進 manifest 的 `.DS_Store` 會被列出來，
使用者看到一個看不懂的項目。

**修正**：manifest 讀出後同樣過濾 `.DS_Store`／`Thumbs.db`。

### 實測
模擬「已安裝＋plugin 已下載並登記＋舊版 manifest 夾帶 .DS_Store」的環境：
- 移除清單乾淨（hooks 4 項、skills 5 項，無 `.DS_Store`）
- 正確列出已下載的 plugin 及其兩處位置，並附上移除指令
- settings 還原後與安裝前完全一致，`autoInstallEnabledPlugins` 已移除
- 無 plugin 的乾淨環境不會誤報

## v1.1（2026-08-05）收窄敏感檔樣式，並區分兩種保護強度

處理 v1.0 留下的「待決定」項目（來自實機回報的發現 1）。

### 問題
`PROTECTED_FILE_PATTERNS` 用 `/credential/`、`/secret/`、`/password/`、`/token/` 這類**子字串**比對，
且命中一律回 `deny`（使用者無法覆寫）。裝到使用者層級後在**所有專案**生效，
於是 `token.py`、`tokenizer.py`、`secrets.py`、`credentials.py`、`get_token.js`
這類再普通不過的原始碼全部讀不到。

### 修正一：樣式收窄為精準匹配
四個寬鬆的英文子字串改為只匹配「看起來真的是密鑰檔」：
`.env*`、`credentials.{json,yaml,…}`、`secrets.{yaml,json,…}`、
`*.{pem,key,p12,pfx,keystore,jks}`、`id_rsa`／`id_ed25519`、`*token*.json`、`.npmrc`／`.pypirc`。
中文三個（個資／捐款／名冊，另補「個人資料」）維持原樣。

### 修正二：區分兩種保護強度（原提案的延伸）
| 對象 | 強度 | 理由 |
|---|---|---|
| 受保護**資料夾**（`_private`／`_raw`／`raw_data`） | `deny` 直接拒絕 | 使用者刻意放進去的位置，零誤判可能 |
| 疑似密鑰／個資的**檔名** | `ask` 人工確認 | 即使收窄仍可能誤判；`deny` 讓使用者連覆寫機會都沒有 |

`ask` 的說明文字依類型分流：密鑰類提醒「內容會進入對話，建議拒絕」；
個資類則說明「若只是檔名剛好命中、內容沒有個資，可以允許」。

### 實測
- 回報中被誤擋的 7 個普通原始碼（`token.py`、`secrets.py`、`credentials.py`…）→ **全部放行**。
- 8 個真正的密鑰檔（`.env`、`credentials.json`、`*.pem`、`id_rsa`、`api-token.json`…）→ **全部 ask**。
- 受保護資料夾（含 Windows 反斜線路徑）→ **維持 deny**，無回歸。

### 連帶更新
`CLAUDE.md` 安全底線第二條拆為 (a) 資料夾一律拒絕 /（b）檔名先問；
`safety-check` 的 A 類拆為 A1／A2 並各自給話術；`_private`／`_raw` 的 README 加註保護強度差異。

## v1.0（2026-08-05）修正致命安裝失敗（依實機回報）

> 來源：Claude Code 於真實 macOS 環境實測後提出的《修正說明-給Cowork》。
> 五項指定修正（A–E）全數套用，另主動修掉其中一項「尚未處理的發現」。

### 致命問題：`enabledPlugins` 格式假設錯誤（100% 失敗）

`enabledPlugins` 有兩種格式，而 **Claude 實際寫入 `~/.claude/settings.json` 的是對照表**：

| 格式 | 樣子 | 出現在哪 |
|---|---|---|
| 陣列 | `["a@b"]` | 本 bundle 的 settings.json |
| **對照表** | `{"a@b": true, "c@d": false}` | **使用者實機的 settings.json** |

原 `unionKeep()` 對物件做展開 → `mine is not iterable` → **只要機器裝過任何 plugin，安裝必定失敗**，
且失敗點在「備份之後、合併之前」，會留下兩個孤兒備份檔而什麼都沒裝成。

### 已套用的修正

- **A**：新增 `pluginKeys()`／`isPluginMap()`／`mergePlugins()`；`unionKeep()` 對非陣列一律防禦。
  **輸出沿用使用者原本的格式**，不強制轉型。
- **B**：`mergeSettings()` 改用新 helper；permissions 計數不再依賴 `.length`。
- **C**：`verifyNoClobber()` 改為格式無關，並新增「**plugin 開關被更動**」檢查——
  防止把使用者刻意設成 `false` 的 plugin 偷偷打開。
- **D**：`uninstall.mjs` 無備份時的逐項移除分支同樣支援兩種格式（原本會 `filter is not a function`）。
- **E**：`copyDir()` 過濾 `.DS_Store`／`Thumbs.db`，不再裝進使用者設定區、也不再誤報項目數。

### 額外修正（來自「尚未處理的發現」第 2、3、4 項）

- **同名 skill／hook 會被無聲覆蓋且還原時被刪除**（發現 2）——這與「不會動到你原有的設定」的承諾牴觸。
  → 新增 `~/.claude/.mymate-manifest.json` 安裝紀錄：
  安裝時若偵測到**不是本包裝的**同名項目，先備份為 `<名稱>.before-mymate` 並提示使用者；
  還原時**只移除紀錄在案的項目**，並把備份放回原位。
- **deny 相對路徑語意**（發現 3）→ 在 `_config.mjs` 加註說明。
- **dry-run 措辭誤導**（發現 4）→ 試跑時改印「（試跑）將會保存…」，不再讓人以為動了檔案。

### 實機情境實測（重現回報環境）

以「`enabledPlugins` 為對照表 + 含刻意關閉的 false + 自訂 `extraKnownMarketplaces` +
自有 hook + **與本包同名的 `plain-speak` skill**」的家目錄測試：

- dry-run 與實際安裝均不再崩潰，安全檢查通過。
- 10 項逐項核對全過：格式維持對照表、`false` 開關未被翻開、
  `extraKnownMarketplaces`／`statusLine` 原封不動、使用者 hook 逐字不變、無 `.DS_Store`、未寫入 `_note`。
- 使用者原有的 `plain-speak` 被備份保留，**還原後完整放回**。
- 還原後 settings 與安裝前**完全一致**，manifest 清除。

### 待你決定（未自動處理）

`PROTECTED_FILE_PATTERNS` 目前是子字串比對且回 `deny`（使用者無法覆寫），
裝到使用者層級後，`token.py`、`secrets.py`、`credentials.py` 這類普通原始碼在**所有專案**都會被擋。
回報建議改為精準匹配（`.env*`、`credentials.*`、`secrets.y?ml`、`*.pem`、`id_rsa`、`*token*.json`），
中文三個（個資／捐款／名冊）維持原樣。**尚未套用，等待決定。**

## v0.11（2026-08-05）身分定名：MYmate

**變更**
在這個資料夾裡，助理的身分定為 **MYmate——使用者的 AI 使用小幫手**，全程自稱 MYmate。

固定開場（`my-first-run` 的第一句話）：

> 「嗨，我是 MYmate，你的 AI 使用小幫手，接下來我會幫你完成 Claude Code 的一些前置準備。」

**誠實原則（一併寫入）**
若使用者直接問「你是不是 Claude」，**據實說明**：
「我是 Claude，MYmate 是這套設定的名字，等於是為你調整過的版本。」
不否認、不含糊帶過——**誠實比人設重要**。這對 NPO 情境的信任特別關鍵。

**檔案異動**
- `CLAUDE.md`：新增「你在這裡的身分：MYmate」段落（含開場範本與誠實原則）。
- `my-first-run`：開場改為先自我介紹，再用三點說明流程。
- 其餘四個 skill：標題下加註自稱規則，避免中途切換稱呼。
- `README.md`：說明打開後會有 MYmate 帶著做前置準備。

## v0.10（2026-08-05）my-install 開場修正

**實測回報的兩個問題**

1. **「你今天才第一次用」這道門檻多餘**
   原本會建議使用者「先用幾天再裝比較安全」。但既然隨時可以完整還原，
   這道建議只是徒增摩擦與猶豫。→ **整段移除**，不再勸他等。

2. **沒有列出會安裝什麼**
   原開場只說「安裝護欄」，沒有告訴使用者具體什麼東西會進到他的電腦。
   對非技術使用者而言，這是最該講清楚的一件事。
   → 新增「**第二件：具體會裝什麼進去（不可略過）**」，以白話表格列出：

   | 裝什麼 | 做什麼用 |
   |---|---|
   | 三個自動檢查哨 | 刪除前攔一下／擋個資與原始資料／寄出前確認 |
   | 五份工作說明書 | 陪伴、白話翻譯、危險動作處理、還原 |
   | 一組保護規則 | 直接禁止幾個危險指令 |
   | 一個小外掛 | 看資料夾適合搭配什麼工具 |

   並明講：放在 `~/.claude`（不是系統資料夾）、都是文字檔、
   **不會動原有設定、不會碰他的檔案**。
   細節（settings.json、hooks 內容）僅在使用者想看時才展開。

**開場結構調整為**：意義 → 裝什麼 → 可還原 → 問副作用可否接受。
（原為：意義 → 用過了嗎 → 副作用 → 可還原）

## v0.9（2026-08-05）新增 my-uninstall skill

**問題**
`install.mjs` 有對應的 `uninstall.mjs`，但 skill 層只有 `my-install`——
還原流程藏在它的內文裡。使用者說「我想移除」時**不一定觸發得到**，
且缺少對稱性。

**新增 `my-uninstall`**
獨立的還原引導，核心原則是「**不要挽留**」：不問「為什麼不用了」拖延、不列好處說服。
開場先讓使用者安心：「你自己的檔案和工作內容都不會被動到。」

流程重點：
- **先釐清移除程度**：只想拿掉自動攔阻／整個都不要／
  其實只是被擋太多想調鬆（第三種很常見，先問清楚可能只需改 `_config.mjs`，
  但使用者仍堅持就照做，不再勸）。
- **先確認有沒有裝過**：沒裝就不要跑腳本，直接告訴他「刪掉資料夾即可，不會留下東西」。
- 還原後**主動說明**「你原本自己的設定都還在」——這是最容易讓人不安的點。
- 交代兩件剩下的事：外掛需另外移除、資料夾可自行刪除
  （並提醒先確認 `_private/`、`_raw/` 裡沒有他自己的資料）。

**連帶調整**
- `my-install` 的還原段改為指向 `my-uninstall`，description 也移除還原職責，避免觸發衝突。
- `CLAUDE.md` 的 skill 對照表新增一列。
- `README.md` 的能力表與資料夾結構同步更新。

## v0.8（2026-08-05）修正「第一步就嚇到使用者」

**問題（實測回報）**
輸入 `/my-first-run` 後，Claude 把四項環境檢查串成一條長指令送出，畫面出現：

```
This command uses shell operators that require approval for safety
cd "…" && echo "--- settings.json ---" && ls -1 … || echo "缺少…"; …
```

對第一次使用的人，這是**引導的第一個畫面**——資訊少、看不懂、還帶紅字警告，
造成的緊張正是這個包要消除的。等於引導在第一步就製造了它要解決的問題。

**三個根因與修法**

1. **指令被串接**：`&&`／`||`／`;` 會觸發系統額外的「shell operators」安全警告。
   → 明訂**一次只送一個簡單指令**，禁止串接。
2. **事前沒有預告**：視窗突然跳出，使用者沒有心理準備。
   → `my-first-run` 新增 **2-0 步（不可略過）**：跑任何指令前先說明
   「會跳視窗、那是正常的、按『允許一次』」。並註明「沒先講就跑指令＝這一步失敗」。
3. **該用檔案工具卻用了 bash**：`ls`／`cat` 會觸發確認，讀檔工具通常不會。
   → 檢查檔案是否存在、讀取內容一律改用檔案工具；僅 `node --version` 與
   `ls ~/.claude/hooks` 兩項需要指令，分開執行且各自先預告。

**其他調整**
- 若使用者不願允許某個檢查指令，**不得堅持**：直接視為「攔阻尚未啟動」繼續流程。
- 指令附帶的說明文字要寫成中文目的（「看看工具檔案在不在」），
  不要留英文技術描述（如 `Check environment readiness`）。
- `CLAUDE.md` 新增「執行指令的規矩」四條（預告／不串接／能不用就不用／說明寫人話）。
- `plain-speak` 新增「在視窗跳出**之前**就預告」段落，含第一次跳出時的安撫話術。
- `safety-check` 新增步驟 2b：規範送出指令的形式。

**註**：無法用 `permissions.allow` 事先放行這些指令——專案層級設定在桌面 App 不生效
（見 v0.5）。因此只能從「少觸發、先預告、看得懂」三個方向處理。

## v0.7（2026-08-05）安裝絕不覆蓋使用者原有設定

**修正三個會動到使用者設定的 bug**

1. **hook 路徑改寫波及使用者的 hook**：原本用 `node .claude/hooks/` 的正則全域替換，
   若使用者自己的 hook 也用相同相對路徑，會被一併改寫指向家目錄。
   → 改為以**我們的檔名**（`block-delete/block-sensitive/confirm-send.mjs`）精準辨識，只改我們自己的。
2. **頂層展開無聲覆蓋**：原本 `{...current, ...incoming}`，我們有定義的鍵會蓋掉使用者的值。
   例如使用者設 `autoInstallEnabledPlugins: false` 會被改成 `true`。
   → 改為**外科手術式合併**：只碰 `enabledPlugins`／`permissions.deny·ask·allow`／`hooks`，
   且一律「聯集追加」不覆蓋；`autoInstallEnabledPlugins` 僅在使用者**完全沒設過**時才寫入。
3. **`_note` 污染使用者設定**：安裝時會把本包的 `_note` 寫進去。→ 合併前先剔除。

**新增安全機制**
- `verifyNoClobber()`：寫檔**之前**逐項驗證——使用者原有的頂層欄位、plugin、
  deny/ask/allow 規則、既有 hook 是否都完整保留。任一項不通過就**中止且不寫檔**。
- 安裝輸出改為逐項列出「實際新增了什麼」，而非只報總數。

**修正重複安裝導致還原失效**
原本每次安裝都建時間戳備份，還原取「最新」那份——重複安裝後最新快照已含本包設定，
還原等於還不回去。
→ 新增 `settings.mymate-original.json`（**只在第一次安裝時建立，之後不覆寫**）保存安裝前原貌；
`uninstall.mjs` 優先使用它還原。時間戳快照仍保留作為額外保險。

**實測（模擬使用者已有複雜設定：自訂 model／statusLine／巢狀自訂欄位／
自有 plugin／自有 deny 規則／兩支使用自身相對路徑的 hook）**
- 12 項逐項檢查全數通過：無任何原有設定被更動或覆蓋。
- 重複安裝 3 次：hook 與規則不累積重複。
- 安裝 3 次後還原：與安裝前**位元組完全一致**。

## v0.6（2026-08-05）skill 化重構：陪伴 + 安全程序

**設計調整**
- `CLAUDE.md` 回歸「**講述這個包的目的與引導**」為主：保留精簡的安全底線
  （受保護路徑、必須調用 `safety-check`、不迴避判斷），詳細程序移進 skill 以便迭代。
  *取捨說明*：CLAUDE.md 是唯一保證載入的檔案，因此安全底線不能全部搬進 skill；
  採「CLAUDE.md 定底線與強制調用、skill 存放可迭代的程序」。
- `my-setup` 拆成兩個 skill，職責分離：
  - **`my-first-run`**（原階段一）——在專案範圍體驗一次、建立信心。
    新增最重要的一步：**用使用者自己的檔案完成一件真正有用的事**（沒完成等於引導失敗）。
  - **`my-install`**（原階段二）——把護欄裝到整台電腦，並負責還原。
    明確定位為「自動攔阻唯一的啟用方式」，且不主動推銷。

**新增 skill（供後續持續優化）**
- **`plain-speak`**——把術語與正在發生的事翻成白話，並提供陪伴節奏
  （開始前說步驟、每步回報、卡住時給兩個選擇、出錯不貼原始訊息）。
  內含可持續補充的術語對照表；專門處理「權限確認視窗該不該按」的求助。
  *目的：讓小白覺得像有人陪著他。*
- **`safety-check`**——危險動作送出前的檢查程序：分類（受保護資料／破壞性／對外送出／
  變更設定）→ 依類別處理（直接拒絕／列清單確認／給完整預覽）→ 說明寫法要求。
  *目的：專案層級沒有自動攔阻時，用程序化的 skill 補上，讓保護行為一致、不因對話漂移。*

**命名說明**
`my-first-exp` 改名為 `my-first-run`（exp 易誤讀為 expert/experiment）。

**檔案異動**
- 新增 `.claude/skills/{my-first-run,my-install,plain-speak,safety-check}/SKILL.md`
- 移除 `.claude/skills/my-setup/`
- `CLAUDE.md`、`README.md`、`settings.json` 的 `_note` 同步更新

## v0.5（2026-08-05）診斷更正：hook 可用，但只認使用者層級

**實測方法**
用「不依賴 node、不含相對路徑」的最小 hook（`date >> 絕對路徑`）分三次測試，
並改用 `UserPromptSubmit`（送出訊息即觸發）以排除「沒用到工具」的干擾：

| 測試 | 層級 | 結果 |
|---|---|---|
| PreToolUse | 專案層級 | ❌ 未觸發 |
| UserPromptSubmit | 專案層級 | ❌ 未觸發 |
| UserPromptSubmit | **使用者層級** | ✅ **觸發成功**（2026-08-05 11:14） |

**結論（更正 v0.4）**
桌面 App **會執行 hook**，但只讀取使用者層級 `~/.claude/settings.json`；
專案層級 `.claude/settings.json` 被整份排除（host 以 `--setting-sources user` 啟動），
這也是 `permissions.deny`／`ask` 一併失效的原因。

v0.4 曾誤判為「桌面 App 不執行 hook」，並據此寫下「階段二對 hook 沒有幫助」——**兩者都錯**。
（附帶更正：petdex 的沉默無法作為證據，因其 hook 會呼叫本機 port 7777 的服務，
服務未啟動時靜默結束且不寫入 `runtime/`，與 hook 是否被觸發無關。）

**設計影響（重要）**
階段二從「選用的擴大範圍」升級為「**技術攔阻唯一的啟用途徑**」。
對桌面 App 使用者而言，只做階段一等於沒有技術層攔阻。

**變更**
- `settings.json` 的 `_note`、三支 hook 檔頭：改為說明「使用者層級才生效」與 `install.mjs` 的必要性。
- `my-setup/SKILL.md`：判準由「作業系統／entrypoint」改為「**hook 是否已裝到使用者層級**」；
  1-2b 改為可主動提及階段二（因它是啟用途徑），但仍讓使用者自選；
  2-0 新增階段二真正價值的說明。
- `CLAUDE.md`：區分「階段二前＝規則保護」與「階段二後＝技術攔阻＋規則」兩種狀態。
- `README.md`：改以「自動保護需要多一個步驟才啟動」呈現，並更正兩階段對照表。

**仍待驗證**
- [ ] `permissions`（deny／ask）在使用者層級是否同樣生效（本次只驗證了 hook）
- [ ] Windows 環境是否同樣「使用者層級有效、專案層級無效」

## v0.4（2026-07-30）安全機制改以 CLAUDE.md 為主防線

**背景（實測發現）**
在 Claude 桌面 App 環境（`CLAUDE_CODE_ENTRYPOINT=claude-desktop`）下實測確認：
`.claude/settings.json` 的 `permissions`（deny／ask）與 `hooks` **完全不生效**。
原因是 host 以 `--setting-sources user` 啟動 Claude CLI，專案層級設定被排除。
旁證：使用者原有的 user 層級 hook（petdex）同樣自 5/20 起無任何活動。
相關官方 issue：anthropics/claude-code #63360、#47993、#27398；desktop/desktop #22138。

**影響**
目標使用者（技術小白）將使用桌面 App，因此**原本以 hook 為核心的安全設計在實際環境中無效**。

**變更**
- `CLAUDE.md` 重寫為**主防線**：新增四條硬性安全規則（受保護路徑一律拒絕、危險動作前先白話說明、
  不迴避風險判斷、對外送出先給預覽），並明確告知 agent「本資料夾的保護完全依賴你遵守這些規則」。
- `my-setup/SKILL.md`：
  - 1-2 步新增**環境偵測**（讀 `CLAUDE_CODE_ENTRYPOINT`）。
  - 新增 1-2b「誠實告知保護現況」，**禁止給使用者虛假安全感**。
  - 1-4 驗證步驟依環境分流：CLI 實測攔阻；桌面 App 改為示範「我會遵守規則」並說明限度。
  - 階段二加註：桌面 App 下擴大範圍**不會讓 hook 復活**，若目的是加強保護則建議不要做。
- 三支 hook 檔頭、`settings.json` 的 `_note`：加註「僅終端機 CLI 生效」。
- `_private/README.md`、`_raw/README.md`、`README.md`：以表格說明兩種環境的保護差異，
  並強調「真正機密的資料不要放進這個資料夾」。

**設計原則調整**
安全的主防線由「hook 技術攔阻」改為「**資料隔離 ＋ CLAUDE.md 行為約束 ＋ App 內建確認**」。
hook 保留給終端機使用者，作為額外一層。

## v0.3（2026-07-30）設定改為明確兩階段

**變更**
- `my-setup/SKILL.md` 重構為**兩階段**：
  - 階段一（預設）＝專案範圍，只在該資料夾生效，並明確告知使用者這一點；
  - 階段二（僅在使用者主動要求時）＝全機範圍，需先確認前提、告知副作用、跑 `--dry-run`、取得同意後才安裝。
  - 階段一結尾只「輕描淡寫提一句」階段二存在，不主動推銷、不當場詢問。
- `README.md`：改以兩階段呈現，階段二加上「護欄會在所有專案生效」的事前告知與還原指令。
- `CLAUDE.md`：新增「設定的兩個階段」段落，要求 agent 不得把兩階段合併進行。

**理由**
降低第一次使用的心理負擔與實際風險：先在可丟棄的範圍內建立信任，確定合用後才擴大到整台電腦。

## v0.2（2026-07-30）可還原性與可見性

**新增**
- `uninstall.mjs`：完整還原全機級安裝——從最新備份還原 settings、刪除本包的 hooks 與 skill；
  若找不到備份，改為「逐項移除本包加入的設定」。支援 `--dry-run`。
- 已實測 install → uninstall 往返：settings.json 位元組完全一致，使用者自有的 hook／skill／自訂欄位均保留。

**修正**
- `install.mjs` 結尾的還原提示改為指向 `uninstall.mjs`（原本只提到還原 settings，未涵蓋複製進去的檔案）。
- `README.md` 新增「看不到 .claude 資料夾是正常的」說明（Mac／Windows 顯示隱藏檔的方法），
  並說明可直接請 Claude 檢查安裝狀態，不必自己翻資料夾。

## v0.1（2026-07-20）第一版
初版骨架，依《小白零思考上手 Claude Code：規劃與設計》建置。

**內容**
- `.claude/settings.json`：`enabledPlugins`（claude-code-setup）、`permissions` 的 deny／ask／allow 規則、三支 hook 註冊。
- `.claude/skills/my-setup/`：開場引導與安裝確認（唯一自製 skill）。
- `.claude/hooks/`：`_config.mjs`（共用設定）＋ `block-delete`／`block-sensitive`／`confirm-send`，全部 Node.js、雙平台通用。
- `install.mjs`：全機級安裝，`os.homedir()` 自動判斷平台，settings 合併不覆蓋且先備份，支援 `--dry-run`。
- `CLAUDE.md`：要求 agent 在風險動作前用白話說明。
- `.gitattributes`、`_private/`、`_raw/`。

**待驗證（尚未在真實環境實測）**
- [ ] `autoInstallEnabledPlugins` 欄位行為與名稱（較新功能）
- [ ] hook 的相對路徑 `node .claude/hooks/*.mjs` 在 Windows 是否正確解析（必要時改用 `$CLAUDE_PROJECT_DIR`）
- [ ] `matcher` 的正則語法（`mcp__.*`、`Read|Write|Edit`）是否如預期匹配
- [ ] macOS 與 Windows 各跑一次完整流程（下載 → 開啟 → 自動安裝 → my-setup → 觸發護欄）

**下一版可能加入**
- 個資內容偵測（不只看檔名，也看內容關鍵字）
- 更多預裝 plugin（依團隊實際使用的服務，如 Asana／Box）
- 私有 marketplace 統一版本
